import { expect } from 'chai';

describe('Test system', () => {
    it('should work', () => {
        const result = 'hello'
        const expected = 'hello'

        expect(result).to.equal(expected)
    })
})
